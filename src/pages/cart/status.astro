---
import CartStatus from '../../components/CartStatus.astro';
import { getCartDb } from '../../utils/getCartDb';
import { getCartItemsDb } from '../../utils/getCartItemsDb';
import { getTotalItemQuantity } from '../../utils/getTotalItemQuantity';

export const partial = true;

let count = 0;

const items = await getCartItemsDb(Astro.cookies);
count = getTotalItemQuantity(items);
---

<CartStatus count={count} />
